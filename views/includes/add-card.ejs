<% if (isLoggedIn) { %>
    <form action="/cart" method="post">
        <input style="display: none;" name="productId" type="text" value="<%= product._id %>">
        <button class="btn" type="submit">Add to Cart</button>
    </form>
    <% } %>